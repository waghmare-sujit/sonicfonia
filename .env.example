# Used only for local development/testing scripts (e.g. testing the
# YouTube Data API against a playlist before wiring it into the plugin).
# The plugin itself never reads .env — end users paste their key into
# Settings → Sonicfonia → YouTube Data API key inside Obsidian.

YOUTUBE_API_KEY=
